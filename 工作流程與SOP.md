# 客服訂單追蹤 — 工作流程（技術上線 ＋ 客服日常 SOP）

> 策略：**先在你這一台電腦把整套跑起來、團隊也連得到，順了再搬到台灣/香港主機。**
> 你會的工具剛好夠用：**Cloudflare（讓團隊連）＋ GitHub（程式碼）**；Firebase 用不到。
> 額外只需要：**Python 3**（跑系統）。

---

# 第一部分　技術上線流程

## 階段 0：在你這台電腦跑起來（約 15 分鐘，一次性）

> 以下為 Mac 指令；若你這台是 Windows 跟我說，我換成對應步驟。

**前置**：裝 Python 3（Mac 多半已內建；沒有就 `brew install python` 或到 python.org 下載）。

1. **取得程式**：從 GitHub 下載這個 repo（`git clone` 或下載 ZIP 解壓），終端機 `cd` 進資料夾。
2. **裝套件**（只裝客服系統需要的 5 個小套件）：
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements-cs.txt
   ```
3. **設通行碼**（只有知道的人進得來）：
   ```bash
   echo "JEWELRY_AUTH_TOKEN=取一組長密碼例如kx7Qm2Vp9Lz4" > .env
   ```
4. **建資料庫**：`python scripts/init_cs_db.py`
5. **啟動**：`uvicorn api.cs_app:app --host 0.0.0.0 --port 8000`
6. **開瀏覽器**：`http://localhost:8000/cs.html?token=你的密碼` → 看到看板。
7. **匯入第一份資料**：點「上傳」把 SHOPLINE 報表 `.xls` 丟進去（之後可接自動匯入）。

## 階段 1：讓團隊（含大陸）連到你這台 — Cloudflare Tunnel

> 不用買伺服器、不用開防火牆埠。Tunnel 會給你一個公開 https 網址，指回你電腦的系統。

**最快試版（免網域，馬上能測）**：
```bash
brew install cloudflared
cloudflared tunnel --url http://localhost:8000
```
→ 終端機會吐出一個 `https://xxxx.trycloudflare.com` 網址，團隊用
`https://xxxx.trycloudflare.com/cs.html?token=你的密碼` 就能連（含大陸）。

**正式版（綁你自己的網域，網址固定好記）**：
1. `cloudflared tunnel login`（瀏覽器授權你在 Cloudflare 的網域）
2. `cloudflared tunnel create cs`
3. `cloudflared tunnel route dns cs cs.你的網域.com`
4. 寫 `~/.cloudflared/config.yml` 指向 `http://localhost:8000`
5. `cloudflared tunnel run cs`
6. 團隊用 `https://cs.你的網域.com/cs.html?token=密碼` 連。

> 大陸連 Cloudflare 通常會繞到香港/海外節點，8 人試營運夠順；要更穩再進階段 2。

## 階段 2：順了再搬到台灣/香港主機（未來、無痛）

1. 把 `db/jewelry.sqlite` 一個檔複製到主機。
2. 主機上同樣方式啟動（`DEPLOY-客服系統.md` 有完整步驟）。
3. Cloudflare Tunnel 改在主機上跑（或 DNS 指過去）。
4. 你這台電腦就功成身退。

## 自動化（你這台常開時）
- 用 launchd（Mac）讓 `uvicorn` 與 `cloudflared` **開機自動啟動**、當掉自動重開——需要時我給你範本。
- SHOPLINE 每日自動匯入：用 `scripts/push_orders.py` + cron（見該檔說明），平常零操作。

---

# 第二部分　客服日常 SOP

## 帳號（每人一組，主管發放）
- 主管在「設定 → 員工帳號」幫每位客服建一組**帳號＋密碼**，並設角色：
  - **主管**：可進設定頁、可核准退貨。
  - **員工**：用看板處理訂單，但不能核准退貨。
- 離職的人按「**停用**」即可，不影響其他人；忘記密碼由主管「重設密碼」。

## 每位客服第一次
- 打開網址 → 用主管給你的**帳號＋密碼登入**（建議第一次登入後改密碼）。
- 登入後系統就知道你是誰，之後你的每個動作都會**自動署名**並記在訂單的「購物旅程」上，不用手動填名字。

## 一上班（接班）— 3 步
1. **看儀表板四個數字**：🔴 異常、🟠 逾期未出、🟡 快到期、進行中。
2. **先點 🔴 異常、再 🟠 逾期**，逐張看「下一步」處理（看板已依出貨期限急迫度排序，越急越上面）。
3. 開「**交接班**」分頁，讀上一班的叮嚀、確認要盯的單，勾「**我已接收**」。

## 處理一張單
1. 看板點開該單 → 看右側「**購物旅程**」秒懂發生過什麼、上一手是誰。
2. 推進左側「**目前進度**」下拉（規格／訂製各自的關卡）。
3. 跟客人說了/客人說了什麼 → 「**加入旅程**」記一筆（員工／客人可切換）。
4. 有狀況 → 勾「**標為異常**」＋選類型＋寫下一步（整列變紅、優先看）。
5. 按「**儲存進度**」→ 系統自動記「最後處理人＝你」。
6. 要看 SHOPLINE 細節 → 點訂單號旁「🔗 到 SHOPLINE 叫單」。

## 兩種流程（員工心法）
- **規格品（出貨期 14 天）**：待處理 → 大陸備貨 → 大陸拍照(出貨備查) → 大陸打包出貨 → 已寄台灣(在途) → 台灣品管出貨 → 已完成
- **訂製品（出貨期 45 天）**：待處理 → 畫3D圖 → 待客人確認圖 → 金工製作中 → **成品照待客人確認**（拍美照寄客人，OK 才出） → 大陸打包出貨 → 已寄台灣(在途) → 台灣品管出貨 → 已完成

## 退貨簽核（核准限主管）
1. 客服（任何人）要退 → 該單「**退貨簽核**」按「申請」＋填原因（變橙色「待簽核」）。
2. **只有主管**看得到、按得了「✔ 核准」或「✘ 駁回」（自動記哪位主管簽、何時，並寫進購物旅程）。員工按不到核准鍵。

## 查客人過去
- 訂單詳情右上「👤 **客人歷史**」→ 同一客人過去所有訂單、進度、誰處理過、有沒有退換過。

## 交班（下班）— 3 步
1. 確認自己負責的單「目前進度」都更新到最新。
2. 「交接班」寫：要盯的單號 ＋ 叮嚀（尤其異常單）。
3. 通知接班人。

## 每天資料更新
- **自動**（接好 `push_orders.py` 後）：每天 09:00 / 18:00 自動把 SHOPLINE 訂單拉進來，平常不用管。
- **手動備援**：到「上傳」頁把 SHOPLINE `.xls` 丟進去即可。

## 歸檔
- 訂單「已完成（送達）」滿 **7 天**（退換貨期）→ 自動移入封存資料庫，看板自動清乾淨；要查舊單到「📦 已封存」。

---

# 需要的東西總表

| 項目 | 用途 | 你有了嗎 |
|---|---|---|
| Python 3 | 跑系統 | 裝一次 |
| 員工帳號（每人一組） | 登入、署名、角色權限 | 主管在設定頁建立 |
| Cloudflare 帳號（＋網域） | 讓團隊連到你這台 | ✅ 你會 |
| GitHub | 程式碼（＋之後資料中繼） | ✅ 你會 |
| Firebase | — | ❌ 用不到 |
| 台灣/香港小主機 | 未來正式搬家用 | 之後再說 |
